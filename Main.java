public class Main {
    public static void main(String[] args){
        TicTacToe game = new TicTacToe();
        game.play();
        /* GameTreeNode node = new GameTreeNode();
        node.AI = 2; */

        /* GameBoard gb = new GameBoard();
        System.out.println(gb.evaluate(1));
        gb.print(); */
        /* node.gameBoard = gb.clone();
        node.expandChildren(3);
        GameTreeNode result = node.runMiniMax(true);
        System.out.println("\n" + result.row + "," + result.col);
        result.gameBoard.print(); */
    }
}
